# Video Analysis Application

This application is designed to analyze videos for various metrics and provide insights based on the analysis.

## Installation

To install the required packages, run:
```bash
pip install -r requirements.txt
```

## Usage

```bash
python main.py
```

## Configuration

Edit the `config.yaml` file to customize the application settings.